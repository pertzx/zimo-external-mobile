================================================================
 TASK 12-c - FIX DA PONTE (ponte indisponivel) + TASK 12 COMPLETO
================================================================

O QUE ACONTECEU
---------------
Voce aplicou do zip anterior SOMente os 2 arquivos Java
(DaemonService.java / OverlayService.java). O Java passou a criar o
socket com nome aleatorio oculto (.sysa_<token>.s - stealth correto!),
mas o BridgeClient.cpp C++ do seu build continuou procurando o nome
VELHO /data/local/tmp/stormbridge.sock -> "Ponte=INDISPONIVEL ->
fallback local -> Nenhum processo alvo encontrado". Era exatamente o
seu logcat: Healthcheck OK no .sysa_xxx.s e connect() falhando no
stormbridge.sock.

COMO APLICAR
------------
1. Extraia o zip na raiz do projeto (substitui os 12 arquivos).
2. Se ja aplicou os 2 Java antes: pode substituir de novo (sao
   identicos) ou pular os 2 Java.
3. Build > Clean Project > Rebuild e instala por cima.
4. A 1a execucao o DaemonService recria daemon/socket/pidfile com o
   token salvo e grava o caminho real em files/stormbridge.path; o
   C++ descobre o caminho por esse arquivo. NAO precisa apagar nada
   em /data/local/tmp (o killAllDaemons limpa os rastros velhos).

ARQUIVOS (12)
-------------
C++ (10) - obrigatorio substituir TODOS:
  app/src/main/cpp/Client/Memory/BridgeClient.cpp   <- FIX PRINCIPAL: descoberta do socket (.path) com re-probe + invalidacao em falha de connect + pacote via /proc/self/cmdline
  app/src/main/cpp/Client/Memory/BridgeClient.hpp   <- nova API BridgeClient::InvalidateSocketPath()
  app/src/main/cpp/Daemon/daemon_main.cpp           <- STEALTH: mudo sem --verbose, sem log em disco sem --log, sem fd de /proc/pid/mem aberto pra sempre, masquerade "appstats" (comm+cmdline)
  app/src/main/cpp/Client/Draw/Silent.cpp           <- Forca do Silent (0-100) + Hit Chance % (dado por write com erro humano real) + jitter de ritmo no idle
  app/src/main/cpp/Client/Draw/Draw.cpp             <- filtros do silent: Ignorar Derrubados / Ignorar Bots / Visibilidade
  app/src/main/cpp/Shared/Globals.hpp               <- Silent: +Forca/HitChance/VisibleCheck/IgnoreKnocked/IgnoreBots
  app/src/main/cpp/Client/Config/saveconfig.cpp     <- persistencia dos 5 novos campos
  app/src/main/cpp/Client/Interface/Interface.cpp   <- FIX tooltip das tabs travando toque (virou pixels no draw list, sem janela) + FAB 96px nascendo/voltando no CENTRO + controles Silent na aba
  app/src/main/cpp/Client/main.cpp                  <- JNI nativeShouldCaptureBypass (stream mode)
  app/src/main/cpp/Client/WebPanel/WebPanel.cpp     <- painel web: 5 controles novos na aba Silent

Java (2) - ja aplicados por voce (identicos; pode pular):
  app/src/main/java/com/stormcheats/DaemonService.java   <- token aleatorio persistido, .sysa_<tok>[.s/.p], grava stormbridge.path, killAllDaemons mata rastros velhos
  app/src/main/java/com/stormcheats/OverlayService.java  <- stream mode real: FLAG_SECURE aplicado/removido nas janelas via polling 16ms

COMO TESTAR
-----------
1. Abra o app, entre no jogo (ou o contrario). No logcat procure:
   StormBridge: "conectado a ponte em /data/local/tmp/.sysa_XXXX.s"
   <- se aparecer o caminho .sysa_ e conexão OK, a ponte voltou.
2. ESP/skeleton deve voltar a atualizar e o Silent a escrever.
3. Aba Silent: Forca (padrao 65, 100 = linha reta), Hit Chance
   (padrao 70 - baixe pra 50-60 se ainda der ban), Visibilidade,
   Ignorar Derrubados, Ignorar Bots.
4. Stream mode: liga/desliga e confira em print/gravacao (ESP some).
5. Tabs: toque direto na tab - sem mais textinho travando o toque.
6. FAB: nasce no centro da tela e maior (96px).

VALIDACAO
---------
g++ -fsyntax-only nos 10 TUs C++: 10 ok / 0 erro.
